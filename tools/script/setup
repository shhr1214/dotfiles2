#!/bin/bash

nix-env --install ripgrep
nix-env --install lsd
nix-env --install ghq
nix-env --install tig
nix-env --install peco
nix-env --install tokei

echo '[ghq]' >> $HOME/.gitconfig           
echo 'root = $HOME/src' >> $HOME/.gitconfig
